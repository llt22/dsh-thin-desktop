# dsh-ui-enhancer

Two client-side improvements for the DSH web interface:

- standalone `<thinking>...</thinking>` text blocks rendered by DSH's standard reasoning component;
- `Escape` cancels the running turn globally.

The plugin changes presentation only. It does not rewrite session logs.
